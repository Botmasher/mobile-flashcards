import React from 'react';
import NewQuestion from '../components';

function NewQuestionContainer({ navigation }) {
	return (
		<NewQuestion navigation={navigation} />
	);
}

export default NewQuestionContainer;
